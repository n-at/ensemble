<div class="mb-3">
    <label for="login" class="form-label">Login</label>
    <input type="text" id="login" name="login" class="form-control" value="{{user_control.Login}}" required>
</div>

{% if mode == "create" %}
    <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input type="password" id="password" name="password" class="form-control" value="" required>
    </div>
{% elif mode == "edit" %}
    <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input type="password" id="password" name="password" class="form-control" value="">
    </div>
    <p class="alert alert-info">
        Leave password field blank to keep current value
    </p>
{% endif %}

<div class="mb-3">
    <label class="form-label">User role</label>
    <div class="form-check">
        <input type="radio"
               class="form-check-input"
               name="role"
               id="role-admin"
               value="1"
               {% if user_control.Role == 1 %}checked{% endif %}
        >
        <label for="role-admin" class="form-check-label">Admin</label>
    </div>
    <div class="form-check">
        <input type="radio"
               class="form-check-input"
               name="role"
               id="role-operator"
               value="2"
               {% if user_control.Role == 2 or not user_control.Role %}checked{% endif %}
        >
        <label for="role-operator" class="form-check-label">Operator</label>
    </div>
</div>
<hr>
<div class="mb-3 text-end">
    <button type="submit" class="btn btn-primary">
        Save user
    </button>
</div>
